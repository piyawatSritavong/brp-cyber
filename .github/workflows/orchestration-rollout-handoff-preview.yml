name: orchestration-rollout-handoff-preview

on:
  workflow_dispatch:

jobs:
  preview:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Generate rollout handoff preview (sample tenant UUID)
        run: python scripts/generate_rollout_handoff_preview.py --tenant-id 00000000-0000-0000-0000-000000000001 --limit 20 || true
